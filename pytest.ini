[pytest]
asyncio_mode = auto
filterwarnings =
    # aiosqlite's background thread may race with event-loop teardown
    # in tests — this is harmless and unavoidable without patching aiosqlite.
    ignore::pytest.PytestUnhandledThreadExceptionWarning
