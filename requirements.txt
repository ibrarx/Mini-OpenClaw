# Mini-OpenClaw Python Dependencies
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# LLM providers — install whichever you plan to use. Both are listed here
# so a fresh `pip install -r requirements.txt` works for either backend.
# Switch active provider via LLM_PROVIDER=anthropic|gemini in .env.
anthropic>=0.42.0,<1.0.0
google-genai>=1.0.0,<2.0.0

aiosqlite>=0.20.0,<1.0.0
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0
beautifulsoup4>=4.12.0,<5.0.0
jsonschema>=4.23.0,<5.0.0

# MCP client — consume external MCP tool servers
mcp>=1.0.0,<2.0.0

# Testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0

# Semantic memory search (local embeddings, no API key needed)
sentence-transformers>=2.2.0
numpy>=1.24.0
